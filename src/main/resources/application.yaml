spring:
  application:
    name: demo
  liquibase:
    change-log: classpath:/db/changelog/db.changelog-master.yaml
  datasource:
    url: jdbc:postgresql://localhost:5432/photorate
    username: postgres
    password: "00634724"

server:
  tomcat:
    max-http-form-post-size: 20MB
    max-swallow-size: 20MB
